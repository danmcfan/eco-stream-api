apiVersion: apps/v1
kind: Deployment
metadata:
  name: eco-stream
  labels:
    app: eco-stream
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eco-stream
  template:
    metadata:
      labels:
        app: eco-stream
    spec:
      containers:
      - name: eco-stream
        image: ghcr.io/danmcfan/eco-stream:latest
        ports:
        - containerPort: 80
        env:
        - name: LISTENER_URL
          value: "0.0.0.0:80"
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              name: eco-stream
              key: auth-token
        - name: MINIO_URL
          value: "minio:9000"
        - name: POSTGRES_URL
          value: "postgres://postgres:password@postgres:5432/postgres"
---
apiVersion: v1
kind: Service
metadata:
  name: eco-stream
spec:
  selector:
    app: eco-stream
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
